# Generated test artifacts (do not commit)
web/test-results/
web/test-results/**

# Local autopilot artifacts (do not commit)
.autopilot-tmp/
.autopilot-tmp/**
artifacts/ci-logs/
artifacts/ci-logs/**
artifacts/doc-repair/
artifacts/doc-repair/**

# Dev Vault artifacts (do not commit)
.vault-dev/
.vault-dev/**
backend/.vault-dev/
backend/.vault-dev/**

# Monorepo bootstrap backups (do not commit)
.legacy-git/

# Local autopilot temp artifacts (do not commit)
.autopilot-tmp/
.autopilot-tmp/**

# Claude worktrees (never commit gitlinks)
.claude/worktrees/

# OS noise
.DS_Store

# Node / Web build artifacts (do not commit)
**/node_modules/
**/dist/
**/storybook-static/
**/.lighthouseci/

# Local env files (do not commit)
**/.env.local
**/.env.*.local

# Java/Maven build artifacts (do not commit)
**/target/
**/.m2/

# Generic output folders (do not commit)
**/test-results/
**/logs/
**/telemetry/

# security
/token.txt
# Telemetry source modules (tracked)
!**/src/**/telemetry/
!**/src/**/telemetry/**

# Python artifacts (do not commit)
**/__pycache__/
**/*.pyc
.autopilot-tmp/execution-log/**
.autopilot-tmp/codex-chatlog/**
.autopilot-tmp/flow-mining/**
web/.eslintcache

# Multi-agent worktree coordination registry (runtime state, shared across worktrees via canonical root).
.worktree-registry.json

# Local CI/tool artifact output dir (2026-04-20): workflows write coverage, FEC, and
# observability reports here during dev/CI runs. Never committed to main — regenerable.
# Previously these 5 files ended up as untracked noise in worktrees:
#   - coverage_visibility.v1.{json,md}
#   - feature_execution_contract_ci.v1.json
#   - observability_coverage_matrix.v1.{json,md}
reports/
