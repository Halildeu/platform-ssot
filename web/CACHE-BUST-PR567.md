# Cache-Bust Marker — PR #567

Frontend-image GHA cache busted via web/ context hash change (Buildx cache-from key derived from COPY content).

Markdown extension matches policy `exclude_globs: **/*.md` so muavin v3 feature contract scope detection skips this file (no uncovered_change / uncovered_ui_change errors).
