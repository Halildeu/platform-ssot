{
  "version": "1.0",
  "allow_direct_merge_default": false,
  "rollback_manual_requires_confirm": true,
  "notes": [
    "Direct merge yalnız break-glass (explicit flag/env) ile.",
    "Rollback yalnız failure|timed_out (workflow_run) veya workflow_dispatch + confirm=ROLLBACK ile."
  ]
}
