name: Auto Fix

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  auto-fix:
    runs-on: ubuntu-latest

    steps:
      - name: Disabled by Local SSOT Policy
        run: |
          set -euo pipefail
          echo "::notice title=auto-fix::disabled by Local SSOT policy (use local autopilot scripts)"
